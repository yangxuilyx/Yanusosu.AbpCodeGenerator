using System;
using System.ComponentModel.DataAnnotations;
using Volo.Abp.Application.Dtos;

namespace @(Model.Namespace).Dtos
{
    /// <summary>
    /// 添加@(Model.DisplayName)Dto
    /// </summary>
    public class Create@(Model.Name)Dto
    {
        @{
            foreach(var item in Model.MetaColumnInfos){
                if(item.IsCreateVisible){
      @:/// <summary>
      @:/// @(item.DisplayName)
      @:/// </summary>
      if(item.MaxLength.HasValue){
      @:[MaxLength(@(item.MaxLength))]
      }
      if(item.Required){
      @:[Required(ErrorMessage = "@(item.DisplayName)不能为空")]
      }
      @:public @(item.StrDataType) @(item.Name) { get; set; }
@: 
 }
            }
        }
     
    }
}